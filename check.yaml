paths:
    - pocketwalk/**/*.py

checks:
    - [symilar, --ignore-imports]
    - [pylint, '{changed}']
    - [flake8]
    - [dodgy]
    - [mypy, --strict, --ignore-missing-imports]
    - [python, -c, 'import pocketwalk.test_pocketwalk as p; p.test_loop()']
