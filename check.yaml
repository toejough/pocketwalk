paths:
    - pocketwalk/**/*.py

checks:
    - [symilar, --ignore-imports]
    - [pylint, '{changed}']
    - [flake8]
    - [dodgy]
    - [mypy, --strict, --ignore-missing-imports]
    #- [pytest, -v]
